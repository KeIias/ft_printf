#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int ac, char **av)
{
	int n;
	float m = 12.21;

	char *str = strdup("qwer");
	//n = atoi(av[2]);
	//printf(av[1], n);
	printf("%-1s", str);
	return (0);
}
